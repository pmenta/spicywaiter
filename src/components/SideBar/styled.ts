import styled from "styled-components";

export const SideBarContainer = styled.nav`
  height: 100vh;
  width: 108px;

  position: fixed;
  z-index: 1;
  top: 0;
  left: 0;

  overflow-x: hidden;
  background-color: #fff;

  padding-top: 40px;

  display: flex;
  justify-content: center;

  box-shadow: 16px 4px 92px -20px rgba(0,0,0,0.2);
`;
